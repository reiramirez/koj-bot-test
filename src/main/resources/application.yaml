server:
  ssl:
    enabled: false
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ExamDatabase
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect